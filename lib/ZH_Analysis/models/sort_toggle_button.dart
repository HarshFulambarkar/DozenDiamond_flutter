class SortToggleButton {
  int? buttonId;
  String? buttonTitle;
  bool? orderIncreasing;
  bool? selected;

  SortToggleButton(
      this.buttonId, this.buttonTitle, this.orderIncreasing, this.selected);
}
